ktor {
    development = true
    deployment {
        watch = [ classes ]
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.example.ApplicationKt.module ]
    }
}

storage {
  ehcacheFilePath = build/ehcache
}